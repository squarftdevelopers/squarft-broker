import { Text,View,TextInput,TouchableOpacity,Image,KeyboardAvoidingView,TouchableWithoutFeedback,Keyboard,Platform,ScrollView,ActivityIndicator } from "react-native";
import { StatusBar } from "expo-status-bar"; import { Redirect,router } from "expo-router"; import { useEffect } from "react"; import { useDispatch,useSelector } from "react-redux"; import { clearError,setName,setOtpFlow,sendOtpApi } from "../../store/slices/authSlice";
const logo=require("../../assets/icons/app-icon.png");
export default function Register(){const dispatch=useDispatch();const {name,mobile,loading,error}=useSelector(s=>s.auth);useEffect(()=>{dispatch(clearError())},[dispatch]);if(!/^\+91\d{10}$/.test(mobile))return <Redirect href="/login"/>;const submit=async()=>{if(!name.trim()||loading)return;dispatch(setOtpFlow("register"));const r=await dispatch(sendOtpApi({phone:mobile,purpose:"register"}));if(sendOtpApi.fulfilled.match(r))router.push("/otp-verification")};return <KeyboardAvoidingView className="flex-1" behavior={Platform.OS==="ios"?"padding":"height"}><TouchableWithoutFeedback onPress={Keyboard.dismiss}><View className="flex-1"><StatusBar style="light"/><View style={{paddingTop:64,paddingBottom:40,paddingHorizontal:24,backgroundColor:"#4A43EC"}}><View style={{width:60,height:60,overflow:"hidden",marginBottom:1}}><Image source={logo} style={{width:110,height:110,margin:-26}} resizeMode="contain"/></View><Text className="text-white text-[26px] font-manrope-bold mb-5">Complete Login</Text><Text className="text-white/80 text-[14px]">Tell us a little about yourself</Text><TouchableOpacity onPress={()=>router.replace('/login')}><Text className="text-white text-[14px] mt-3 underline">{mobile} · Change number</Text></TouchableOpacity></View><ScrollView className="flex-1 bg-white" contentContainerStyle={{padding:24,paddingTop:32}} keyboardShouldPersistTaps="handled"><Text className="text-gray-500 text-[13px] mb-1.5">Full Name</Text><View className="border border-gray-200 rounded-xl px-4 py-2 mb-5"><TextInput value={name} onChangeText={v=>dispatch(setName(v))} placeholder="Full Name" placeholderTextColor="#aaa" autoCapitalize="words" className="text-[15px] text-black" autoFocus/></View>{error&&<Text className="text-red-500 text-[13px] mb-4 text-center">{error}</Text>}<TouchableOpacity onPress={submit} disabled={loading||!name.trim()} className="bg-[#4A43EC] rounded-2xl py-4 items-center" style={{opacity:!name.trim()?.5:1}}>{loading?<ActivityIndicator color="#fff"/>:<Text className="text-white text-[16px] font-semibold">Continue</Text>}</TouchableOpacity></ScrollView></View></TouchableWithoutFeedback></KeyboardAvoidingView>}
